# OAuth2 Configuration for Gmail (Optional)
# Get these from https://console.cloud.google.com/
GMAIL_CLIENT_ID=your_gmail_oauth_client_id
GMAIL_CLIENT_SECRET=YOUR_GMAIL_OAUTH_CLIENT_SECRET

# OAuth2 Configuration for Outlook (Optional)  
# Get these from https://portal.azure.com/
OUTLOOK_CLIENT_ID=your_outlook_oauth_client_id
OUTLOOK_CLIENT_SECRET=YOUR_OUTLOOK_OAUTH_CLIENT_SECRET

# Encryption Master Key (Optional)
# If not provided, will be auto-generated and stored locally
MCP_MASTER_KEY=your_random_master_key_for_credential_encryption

# Note: For password-based email accounts (most providers),
# credentials are configured per account via the add_email_account tool.
# OAuth2 credentials above are only needed for advanced Gmail/Outlook integration.